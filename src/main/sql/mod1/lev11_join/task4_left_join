У вас есть две таблицы:
1. Таблица `products`, содержащая информацию о всех продуктах.
2. Таблица `sales`, содержащая данные о продажах.

Напишите SQL-запрос, который выведет полный список всех продуктов и количество их продаж.
Если продукт еще не был продан, в столбце количества продаж должно быть значение `NULL`.


SELECT
    p.product_name, -- Выбираем имя продукта из таблицы products
    SUM(s.quantity) AS total_sold -- Суммируем количество продаж из таблицы sales
FROM
    products p
LEFT JOIN
    sales s
ON
    p.product_id = s.product_id -- Связываем таблицы по product_id
GROUP BY
    p.product_id, p.product_name; -- Группируем данные по идентификатору и имени продукта


product_name	total_sold
Смартфон	    8
Планшет	        2
Ноутбук