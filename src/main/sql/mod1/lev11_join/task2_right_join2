У вас есть две таблицы: `products` — список продуктов, `sales` — список продаж продуктов.
Напишите SQL-запрос с использованием `RIGHT JOIN`, чтобы получить список всех продаж и соответствующих
названий продуктов. Если продукт не найден, в столбце названия должно быть `NULL`.
Выведите следующие столбцы: `sale_id`, `product_id`, `name`, `quantity`.


select
    sales.sale_id,
    sales.product_id,
    products.name,
    sales.quantity
from
    products right join sales
    on
    products.product_id = sales.product_id


